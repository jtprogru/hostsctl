---
title: hostsctl
description: Manage /etc/hosts from a YAML config — groups, zone files, remote blocklists and backups.
template: splash
hero:
  tagline: One YAML config. Everything else in /etc/hosts stays exactly where you left it.
  image:
    # Инлайн, а не file: логотип нарисован через currentColor, и внутри <img>
    # он стал бы чёрным квадратом на тёмной теме.
    html: |
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" fill="none" role="img" aria-label="hostsctl">
        <rect x="1.5" y="1.5" width="29" height="29" rx="7" stroke="currentColor" stroke-width="2" opacity="0.35"/>
        <path d="M7 11h5M15 11h10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
        <path d="M7 16h5M15 16h7" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
        <path d="M7 21h5M15 21h9" stroke="currentColor" stroke-width="2" stroke-linecap="round" opacity="0.4"/>
      </svg>
  actions:
    - text: Getting started
      link: /hostsctl/getting-started/
      icon: right-arrow
    - text: Install
      link: /hostsctl/install/
      variant: secondary
    - text: View on GitHub
      link: https://github.com/jtprogru/hostsctl
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard, Steps } from '@astrojs/starlight/components';

## Thirty seconds

<Steps>

1. Install it and create a config:

   ```bash
   brew install jtprogru/tap/hostsctl
   hostsctl init
   ```

2. Add an entry — it goes into the config, not into `/etc/hosts` yet:

   ```bash
   hostsctl add 127.0.0.1 k8s.orb.local --comment orbstack
   ```

3. Look before you write. `diff` needs no root and changes nothing:

   ```bash
   hostsctl diff
   ```

4. Apply it:

   ```bash
   sudo hostsctl apply
   ```

</Steps>

The result in `/etc/hosts`:

```
127.0.0.1	localhost          ← system lines, untouched
255.255.255.255	broadcasthost

# >>> hostsctl begin >>>
# generated by hostsctl 2026-08-11 17:15
# source: /Users/you/.config/hostsctl/config.yaml — edit that, not this block

# --- local — Local development ---
127.0.0.1      k8s.orb.local  # orbstack
# <<< hostsctl end <<<
```

## Why it works this way

<CardGrid>
  <Card title="Your file stays your file" icon="approve-check">
    Entries are rendered into a block between markers. System lines, other tools' blocks
    and hand edits are carried over byte for byte — verified by tests that run against a
    copy of a real `/etc/hosts`.
  </Card>
  <Card title="Groups you can switch off" icon="setting">
    A group is enabled or disabled as a whole. Turn a blocklist off for an afternoon, turn
    it back on, and nothing in the config was lost in between.
  </Card>
  <Card title="Config split across files" icon="document">
    Keep everything in one `config.yaml`, or spread it over zone files — plain hosts
    syntax or YAML. Edits made through the CLI go back to the file the group came from.
  </Card>
  <Card title="Honest about failure" icon="warning">
    Atomic writes with `fsync` and `rename`, a snapshot before every write, a refusal to
    produce a file without `127.0.0.1 localhost`, and an exit code for every way a run can
    go wrong.
  </Card>
</CardGrid>

## Where to go next

New here? Read the two pages under **Start here**, then come back for whichever guide
matches what you are trying to do.

<CardGrid>
  <LinkCard
    title="Getting started"
    href="/hostsctl/getting-started/"
    description="From an empty config to a rendered /etc/hosts in five commands, and the three ways to undo it."
  />
  <LinkCard
    title="Installation"
    href="/hostsctl/install/"
    description="Homebrew, crates.io, the install script, a release archive, and how to verify a download."
  />
</CardGrid>

### Guides — one task each

<CardGrid>
  <LinkCard
    title="Configuration"
    href="/hostsctl/guides/configuration/"
    description="What lives in config.yaml, how entries map addresses to names, where the file is looked up."
  />
  <LinkCard
    title="Groups"
    href="/hostsctl/guides/groups/"
    description="Sets of entries that switch on and off as a whole, and how to move them between files."
  />
  <LinkCard
    title="Zone files"
    href="/hostsctl/guides/zones/"
    description="Splitting the config across files, in YAML or in plain hosts syntax."
  />
  <LinkCard
    title="Remote blocklists"
    href="/hostsctl/guides/blocklists/"
    description="Attaching a hosts list by URL, the ETag cache, and why apply never touches the network."
  />
  <LinkCard
    title="Backups"
    href="/hostsctl/guides/backups/"
    description="A snapshot before every write, how to roll one back, and what a snapshot is not."
  />
  <LinkCard
    title="Permissions and sudo"
    href="/hostsctl/guides/permissions/"
    description="Which commands need root, and why sudo leaves no root-owned files in your home directory."
  />
</CardGrid>

### Reference — generated from the code

<CardGrid>
  <LinkCard
    title="CLI reference"
    href="/hostsctl/reference/cli/"
    description="Every command and flag, printed by the binary itself. CI fails if it drifts."
  />
  <LinkCard
    title="Config reference"
    href="/hostsctl/reference/config/"
    description="Every key of config.yaml and of a zone file, with its default and its meaning."
  />
  <LinkCard
    title="Validation rules"
    href="/hostsctl/reference/check/"
    description="Everything hostsctl check reports, and whether it stops a write."
  />
  <LinkCard
    title="Exit codes"
    href="/hostsctl/reference/exit-codes/"
    description="What each status means, so a script can tell a broken config from a missing sudo."
  />
</CardGrid>

### Project

<CardGrid>
  <LinkCard
    title="How it works"
    href="/hostsctl/project/how-it-works/"
    description="The managed block, the six steps of a write, and why the timestamp does not change on its own."
  />
  <LinkCard
    title="Migrating from hosts-sync"
    href="/hostsctl/project/migration/"
    description="Two ways across from the older shell script, and what happens to its block."
  />
  <LinkCard
    title="Contributing"
    href="/hostsctl/project/contributing/"
    description="How to build, test and propose a change — including the docs site."
  />
</CardGrid>

## What it is not

hostsctl does not resolve names — `/etc/hosts` does, with all of its limits. Wildcards,
ports and paths in a hostname do not work there, so
[`hostsctl check`](/hostsctl/reference/check/) rejects them instead of letting the system
ignore them silently. For wildcard domains you want dnsmasq or `/etc/resolver/`.
