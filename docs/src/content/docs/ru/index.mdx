---
title: hostsctl
description: Управление /etc/hosts из YAML-конфига — группы, файлы-зоны, удалённые блоклисты и бэкапы.
template: splash
hero:
  tagline: Один YAML-конфиг. Всё остальное в /etc/hosts остаётся ровно там, где вы это оставили.
  image:
    # Симлинк на assets/cover.svg в корне репозитория — обложка одна и та же в
    # README и здесь. Файл, а не инлайн: цвета в нём заданы явно.
    file: ../../../assets/cover.svg
    alt: "hostsctl — управление /etc/hosts из YAML-конфига: группы, файлы-зоны, удалённые блоклисты, бэкапы"
  actions:
    - text: Начать
      link: /hostsctl/ru/getting-started/
      icon: right-arrow
    - text: Установка
      link: /hostsctl/ru/install/
      variant: secondary
    - text: GitHub
      link: https://github.com/jtprogru/hostsctl
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard, Steps } from '@astrojs/starlight/components';

## Тридцать секунд

<Steps>

1. Поставить и создать конфиг:

   ```bash
   brew install jtprogru/tap/hostsctl
   hostsctl init
   ```

2. Добавить запись — она попадает в конфиг, а не сразу в `/etc/hosts`:

   ```bash
   hostsctl add 127.0.0.1 k8s.orb.local --comment orbstack
   ```

3. Посмотреть перед записью. `diff` не требует root и ничего не меняет:

   ```bash
   hostsctl diff
   ```

4. Применить:

   ```bash
   sudo hostsctl apply
   ```

</Steps>

Результат в `/etc/hosts`:

```
127.0.0.1	localhost          ← системные строки, не тронуты
255.255.255.255	broadcasthost

# >>> hostsctl begin >>>
# generated by hostsctl 2026-08-11 17:15
# source: /Users/you/.config/hostsctl/config.yaml — edit that, not this block

# --- local — Local development ---
127.0.0.1      k8s.orb.local  # orbstack
# <<< hostsctl end <<<
```

## Почему так

<CardGrid>
  <Card title="Файл остаётся вашим" icon="approve-check">
    Записи рендерятся в блок между маркерами. Системные строки, блоки других утилит и
    ручные правки переносятся байт в байт — это проверено тестами на копии настоящего
    `/etc/hosts`.
  </Card>
  <Card title="Группы, которые выключаются" icon="setting">
    Группа включается и выключается целиком. Выключил блоклист на день, включил обратно —
    и ничего в конфиге за это время не потерялось.
  </Card>
  <Card title="Конфиг по файлам" icon="document">
    Можно держать всё в одном `config.yaml`, а можно разложить по зонам — в обычном
    hosts-синтаксисе или в YAML. Правки через CLI уходят обратно в тот файл, откуда группа
    пришла.
  </Card>
  <Card title="Честность про отказы" icon="warning">
    Атомарная запись с `fsync` и `rename`, снимок перед каждой записью, отказ отдать файл
    без `127.0.0.1 localhost` и отдельный код выхода на каждый способ упасть.
  </Card>
</CardGrid>

## Куда идти дальше

Впервые здесь? Прочитайте две страницы из раздела **Начало**, а потом возвращайтесь за тем
руководством, которое отвечает на вашу задачу.

<CardGrid>
  <LinkCard
    title="Начало"
    href="/hostsctl/ru/getting-started/"
    description="От пустого конфига до отрендеренного /etc/hosts за пять команд и три способа откатиться."
  />
  <LinkCard
    title="Установка"
    href="/hostsctl/ru/install/"
    description="Homebrew, crates.io, скрипт установки, релизный архив и как проверить скачанное."
  />
</CardGrid>

### Руководства — по одной задаче на страницу

<CardGrid>
  <LinkCard
    title="Конфигурация"
    href="/hostsctl/ru/guides/configuration/"
    description="Что лежит в config.yaml, как записи связывают адреса с именами, где ищется файл."
  />
  <LinkCard
    title="Группы"
    href="/hostsctl/ru/guides/groups/"
    description="Наборы записей, которые включаются и выключаются целиком, и как переносить их между файлами."
  />
  <LinkCard
    title="Файлы-зоны"
    href="/hostsctl/ru/guides/zones/"
    description="Как разложить конфиг по файлам — в YAML или в обычном hosts-синтаксисе."
  />
  <LinkCard
    title="Удалённые блоклисты"
    href="/hostsctl/ru/guides/blocklists/"
    description="Подключение hosts-списка по URL, кеш с ETag и почему apply не ходит в сеть."
  />
  <LinkCard
    title="Бэкапы"
    href="/hostsctl/ru/guides/backups/"
    description="Снимок перед каждой записью, как откатиться и чем снимок не является."
  />
  <LinkCard
    title="Права и sudo"
    href="/hostsctl/ru/guides/permissions/"
    description="Каким командам нужен root и почему sudo не оставляет root-овых файлов в домашнем каталоге."
  />
</CardGrid>

### Справочник — генерируется из кода

<CardGrid>
  <LinkCard
    title="Справочник CLI"
    href="/hostsctl/ru/reference/cli/"
    description="Все команды и флаги, которые печатает сам бинарь. CI падает, если справочник разошёлся."
  />
  <LinkCard
    title="Справочник конфига"
    href="/hostsctl/ru/reference/config/"
    description="Каждый ключ config.yaml и файла-зоны, со значением по умолчанию и смыслом."
  />
  <LinkCard
    title="Правила проверки"
    href="/hostsctl/ru/reference/check/"
    description="Всё, о чём сообщает hostsctl check, и останавливает ли это запись."
  />
  <LinkCard
    title="Коды выхода"
    href="/hostsctl/ru/reference/exit-codes/"
    description="Что означает каждый статус, чтобы скрипт отличил сломанный конфиг от забытого sudo."
  />
</CardGrid>

### Проект

<CardGrid>
  <LinkCard
    title="Как это работает"
    href="/hostsctl/ru/project/how-it-works/"
    description="Управляемый блок, шесть шагов записи и почему отметка времени не меняется сама."
  />
  <LinkCard
    title="Миграция с hosts-sync"
    href="/hostsctl/ru/project/migration/"
    description="Два пути со старого shell-скрипта и что происходит с его блоком."
  />
  <LinkCard
    title="Как участвовать"
    href="/hostsctl/ru/project/contributing/"
    description="Как собрать, протестировать и предложить изменение — включая сайт документации."
  />
</CardGrid>

## Чем это не является

hostsctl не резолвит имена — это делает `/etc/hosts` со всеми своими ограничениями.
Wildcards, порты и пути в имени хоста там не работают, поэтому
[`hostsctl check`](/hostsctl/ru/reference/check/) отвергает их, а не даёт системе молча
проигнорировать. Для wildcard-доменов нужен dnsmasq или `/etc/resolver/`.
