---
title: CLI reference
description: Every command and flag, generated from the argument parser.
---

import { Content as CliReference } from '../../../generated/cli.md';
import { Aside } from '@astrojs/starlight/components';

<Aside type="note" title="Generated">
  This page comes out of the `clap` command tree via `make gen`. CI fails when the
  committed copy differs from a fresh generation, so it cannot fall behind the binary.
</Aside>

<CliReference />

## Also available

`hostsctl --help` prints the same information, `hostsctl <command> --help` narrows it to
one command, and `hostsctl man` prints a man page in roff format.
