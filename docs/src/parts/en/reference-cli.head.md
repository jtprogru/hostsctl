---
title: CLI reference
description: Every command and flag, generated from the argument parser.
sidebar:
  order: 1
editUrl: https://github.com/jtprogru/hostsctl/edit/main/docs/src/parts/en/reference-cli.head.md
---

<!--
  This page is assembled by `make gen`: this header, then the output of
  `hostsctl docs cli`, then docs/src/parts/en/reference-cli.tail.md.
  Edit the parts, never the assembled file.
-->

:::note[Generated from the code]
The command list below comes out of the `clap` command tree via `make gen`. CI fails when
the committed copy differs from a fresh generation, so it cannot fall behind the binary.
:::

