---
title: Exit codes
description: What each exit status means, so a script can tell failures apart without parsing text.
sidebar:
  order: 3
editUrl: https://github.com/jtprogru/hostsctl/edit/main/docs/src/parts/en/reference-exit-codes.head.md
---

<!--
  This page is assembled by `make gen`: this header, then the output of
  `hostsctl docs exit-codes`, then docs/src/parts/en/reference-exit-codes.tail.md.
  Edit the parts, never the assembled file.
-->

:::note[Generated from the code]
The table is printed by the binary itself via `make gen`, so it cannot drift from the
codes the program actually returns.
:::

## The table

